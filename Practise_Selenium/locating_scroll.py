# How to locate scroll-bar on a webpage.
#Idea - to locate aclass like "%scroll% at the last leaf level. and getattribute scrollHeight and scrollOffset
# If scrollHeight > scrolloffset, it has a scrollbar

from selenium.webdriver import we